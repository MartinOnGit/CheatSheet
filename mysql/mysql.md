## Connect to local using all param :

```bash
mysql --host 127.0.0.1 --port 3307 --user <user> --database <db> --password
```
then type password

the load script is intended for update from a local csv from `./data/`